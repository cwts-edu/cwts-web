---
import type { Language } from "@libs/language";
import { getSite } from "@libs/site";
import Base from "@layouts/Base.astro";
import Latest from "@components/home/Latest.astro";

const language: Language = "zh";
const site = getSite(language);
---

<Base title={site.title} language={language}>
  <Latest />
</Base>
